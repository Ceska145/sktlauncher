import 'package:flutter/material.dart';

/// Modern Professional tema renkleri
class AppColors {
  // === LIGHT THEME === //
  
  // Ana renkler
  static const Color primary = Color(0xFF212121); // Koyu gri
  static const Color secondary = Color(0xFF424242); // Orta gri
  static const Color background = Color(0xFFFFFFFF); // Beyaz
  static const Color surface = Color(0xFFF5F5F5); // Açık gri
  
  // Durum renkleri
  static const Color danger = Color(0xFFD32F2F); // Kırmızı (tehlike/uyarı)
  static const Color warning = Color(0xFFFF9800); // Turuncu (dikkat)
  static const Color success = Color(0xFF4CAF50); // Yeşil (güvenli)
  static const Color info = Color(0xFF2196F3); // Mavi (bilgi)
  
  // Metin renkleri
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  static const Color textWhite = Color(0xFFFFFFFF);
  
  // Border & Divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
  
  // Overlay & Shadow
  static const Color overlay = Color(0x80000000);
  static const Color shadow = Color(0x1A000000);

  // === DARK THEME === //
  
  // Ana renkler (Dark)
  static const Color darkPrimary = Color(0xFFBB86FC); // Mor
  static const Color darkSecondary = Color(0xFF03DAC6); // Turkuaz
  static const Color darkBackground = Color(0xFF121212); // Çok koyu gri
  static const Color darkSurface = Color(0xFF1E1E1E); // Koyu gri
  
  // Durum renkleri (Dark - daha parlak)
  static const Color darkDanger = Color(0xFFCF6679); // Açık kırmızı
  static const Color darkWarning = Color(0xFFFFB74D); // Açık turuncu
  static const Color darkSuccess = Color(0xFF81C784); // Açık yeşil
  static const Color darkInfo = Color(0xFF64B5F6); // Açık mavi
  
  // Metin renkleri (Dark)
  static const Color darkTextPrimary = Color(0xFFE0E0E0);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkTextHint = Color(0xFF707070);
  
  // Border & Divider (Dark)
  static const Color darkBorder = Color(0xFF2C2C2C);
  static const Color darkDivider = Color(0xFF2C2C2C);
  
  // Overlay & Shadow (Dark)
  static const Color darkOverlay = Color(0xCC000000);
  static const Color darkShadow = Color(0x33000000);
}

